# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'mechanize'
gem 'relaton-ecma', "~> 2.0.0.alpha.3" # github: 'relaton/relaton-ecma', branch: 'main'
